spring:
  application:
    name: foxtrip

  datasource:
    url: jdbc:postgresql://localhost:5432/foxtrip
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Tạo/cập nhật tables, KHÔNG xóa khi stop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false  # Tắt Flyway
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    mongodb:
      host: localhost
      port: 27017
      database: foxtrip
      username: admin
      password: admin
      authentication-database: admin
      auto-index-creation: true
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis
    redis:
      time-to-live: 3600000

  mail:
    host: localhost
    port: 1025
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/foxtrip
          jwk-set-uri: http://localhost:8080/realms/foxtrip/protocol/openid-connect/certs
        app:
          keycloak:
            server-url: http://localhost:8080
            realm: foxtrip
            client-id: foxtrip-backend
            client-secret: qfeZjYBRwJRCvjR2WAtnvCJKjWcMCZIA
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
server:
  port: 8081

cloudinary:
  cloud-name: dxqfvuf6x
  api-key: 969367144844572
  api-secret: abV5gq8JHvD4sG9j75tbkjK0JE0
# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
# Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
logging:
  level:
    com.haui.foxtrip: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
